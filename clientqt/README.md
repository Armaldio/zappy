# Zappy Client GUI

- QT 5.X
- SFML 2.4.X

# Compilation

```sh
mkdir build && cd build && cmake .. && make
```

# Installation

```sh
cd build && make install
```

__Warning__: texture require ```make install```

